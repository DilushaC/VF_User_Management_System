@model UserManagement.Data.Models.MenuItem

@{
    ViewData["Title"] = "Create Menu Item";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var displayOrder = Model?.DisplayOrder > 0 ? Model.DisplayOrder : 1;
}

<div class="row">
    <div class="row p-2">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h3 class="header">@ViewData["Title"]</h3>
                    <br />

                    <form asp-action="Create" asp-controller="Menu" method="post" id="RegisterForm" novalidate>
                        <fieldset>

                            <!-- Product -->
                            <div id="productContainer" class="row g-2">
                                <div class="mb-3 col-md-6">
                                    <label class="form-label">Select Product</label>
                                    <select id="ProductId"
                                            asp-for="ProductId"
                                            class="form-control select2 round"
                                            data-bs-toggle="tooltip"
                                            data-bs-placement="right"
                                            title="Product is required"
                                            required
                                            data-placeholder="Select Base Product"
                                            asp-items="ViewBag.Products">
                                        <option value="">Select Base Product</option>
                                    </select>
                                </div>
                            </div>

                            <!-- Menu Details (Hidden initially) -->
                            <div id="menuDetailsContainer" class="d-none">

                                <!-- Menu Title -->
                                <!-- Menu Title -->
                                <div class="row g-2">
                                    <div class="mb-3 col-md-6">
                                        <label asp-for="MenuTitle" class="form-label">Menu Title</label>
                                        <input asp-for="MenuTitle" id="MenuTitle" class="form-control round" required />
                                        <span asp-validation-for="MenuTitle" class="invalid-tooltip"></span>
                                        <small id="menuTitle-status"></small>
                                    </div>
                                </div>


                                <!-- Is Main Menu -->
                                <div class="row g-2">
                                    <div class="mb-3 col-md-3 form-check">
                                        <input type="checkbox" class="form-check-input" id="IsMainMenu" name="IsMainMenu">
                                        <label class="form-check-label" for="IsMainMenu">
                                            This is a Main Menu
                                        </label>
                                    </div>

                                    <div class="mb-3 col-md-3 form-check">
                                        <input type="checkbox"
                                               class="form-check-input"
                                               id="NoSubMenus"
                                               name="NoSubMenus">
                                        <label class="form-check-label" for="NoSubMenus">
                                            No Sub Menus
                                        </label>
                                    </div>
                                </div>

                            </div>


                            <!-- Pages -->
                            <div id="pageContainer" class="row g-2">
                                <div class="mb-3 col-md-6">
                                    <label asp-for="PageId" class="form-label">Page Name</label>

                                    <select asp-for="PageId"
                                            id="PageIds"
                                            class="form-control select2 round"
                                            asp-items="ViewBag.Pages"
                                            required>
                                        <option value=""></option>
                                    </select>

                                    <span asp-validation-for="PageId" class="text-danger"></span>
                                </div>
                            </div>


                            <!-- Parent Menu -->
                            <div id="parentMenuContainer" class="row g-2">
                                <div class="mb-3 col-md-6">
                                    <label class="form-label">Select Parent Menu</label>
                                    <select id="ParentMenuItemId"
                                            asp-for="ParentMenuItemId"
                                            class="form-control select2 round"
                                            data-bs-toggle="tooltip"
                                            data-bs-placement="right"
                                            title="Parent Menu required"
                                            required
                                            data-placeholder="Select Parent Menu"
                                            asp-items="ViewBag.ParentMenus">
                                        <option value="">Select Parent Menu</option>
                                    </select>
                                </div>
                            </div>

                            <!-- Icon Class -->
                            <div id="iconClassContainer" class="row g-2 d-none">
                                <div class="mb-3 col-md-2">
                                    <label class="form-label">Icon Class</label>
                                    <input id="IconClass" name="IconClass"
                                           class="form-control round" />
                                </div>
                            </div>

                            <div id="categoryContainer" class="row g-2 d-none">
                                <div class="mb-3 col-md-6">
                                    <label class="form-label">Select Category</label>
                                    <select id="CategoryId"
                                            asp-for="CategoryId"
                                            class="form-control select2 round"
                                            asp-items="ViewBag.Categories">
                                        <option value="">Select Category</option>
                                    </select>
                                </div>
                            </div>

                            <div id="displayOrderContainer" class="row g-2">
                                <div class="mb-3 col-md-2">
                                    <label asp-for="DisplayOrder"
                                           class="form-label d-flex justify-content-between align-items-center">
                                        <span>Display Order</span>
                                    </label>

                                    <input type="number"
                                           asp-for="DisplayOrder"
                                           id="DisplayOrder"
                                           class="form-control w-75"
                                           min="1"
                                           max="20"
                                           step="1"
                                           value="@displayOrder" />

                                    <small id="displayOrder-status" class="d-block mt-1"></small>
                                </div>
                            </div>



                            <!-- Status -->
                            <div class="row g-2">
                                <div class="mb-3 col-md-2">
                                    <label class="form-label">Active Status</label>
                                    <select name="IsActive" class="form-select">
                                        <option value="true">Active</option>
                                        <option value="false">Inactive</option>
                                    </select>
                                </div>
                            </div>

                            <button type="submit" class="btn btn-primary">
                                Create Menu Item
                            </button>

                        </fieldset>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Load jQuery only ONCE -->
<script src="~/assets/lib/jquery/jquery.min.js"></script>
<script src="~/assets/js/sweetalert2.all.min.js"></script>
<!-- Load Select2 JS -->
<script src="~/assets/vendor/select2/js/select2.min.js"></script>

<script>
    let displayOrderValid = true;

    $("#DisplayOrder").on("change", function () {

        let displayOrder = $(this).val();
        let productId = $("#ProductId").val();

        // Guard
        if (!displayOrder || !productId) {
            $("#displayOrder-status").html("");
            displayOrderValid = false;
            return;
        }

        $.ajax({
            url: '/Page/CheckPageLevel', // reuse API if intended
            type: 'POST',
            data: {
                displayOrder: displayOrder,
                productId: productId,
                __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
            },
            success: function (res) {
                if (res.exists) {
                    displayOrderValid = false;
                    $("#displayOrder-status")
                        .html(`<span class="text-danger">❌ Display order already used</span>`);
                } else {
                    displayOrderValid = true;
                    $("#displayOrder-status")
                        .html(`<span class="text-success">✔ Display order available</span>`);
                }
            },
            error: function () {
                displayOrderValid = false;
                $("#displayOrder-status")
                    .html(`<span class="text-danger">⚠ Unable to validate display order</span>`);
            }
        });
    });
</script>

<script>
    $("#ProductId").on("change", function () {
        const productId = $(this).val();

        if (productId) {
            $("#menuDetailsContainer").removeClass("d-none");
        } else {
            $("#menuDetailsContainer").addClass("d-none");

            // Reset values if product cleared
            $("#MenuTitle").val("");
            $("#IsMainMenu").prop("checked", false);
            $("#NoSubMenus").prop("checked", false);
        }
    });
</script>



<script>

    $("#ParentMenuItemId").select2({
        placeholder: "Select Parent Menu",
        allowClear: true
    });

    $("#ProductId").select2({
        placeholder: "Select Product",
        allowClear: true
    });

    $("#PageIds").select2({
        placeholder: "Select Page",
        allowClear: true,
    });

    $("#CategoryId").select2({
        placeholder: "Select Page",
        allowClear: true,
    });

    $("#ProductId").on('change', function () {
        var productId = $(this).val();
        if (productId) {
            $("#PageId").prop('disabled', false);
            $("#PageId").val('');
        } else {
            $("#PageId").prop('disabled', true);   // Keep disabled if no product selected
            $("#PageId").val('');                   // Optional: clear value
        }
    });



    $("#ProductId").on("change", function () {
        let productId = $(this).val();
        if (productId) {
            $.ajax({
                url: '/Permission/GetPagesByProduct',
                type: 'GET',
                data: { productId: productId },
                success: function (response) {
                    if (response.success) {
                        $("#PageIds").empty();
                        response.data.forEach(page => {
                            $("#PageIds").append(
                                new Option(page.pageName, page.id)
                            );
                        });
                        $("#PageIds").trigger('change');
                    }
                }
            });
        } else {
            $("#PageIds").empty().trigger('change');
        }
    });


    let menuTitleValid = true;

    $("#MenuTitle").on("input change", function () {
        const menuTitle = $(this).val().trim();
        const productId = $("#ProductId").val();
        const status = $("#menuTitle-status");

        status.text("").removeClass("text-danger text-success");

        // Basic guard checks
        if (!menuTitle || !productId) {
            menuTitleValid = false;
            return;
        }

        $.ajax({
            url: "/Menu/CheckMenuTitle",   // API endpoint
            type: "POST",
            data: {
                ProductId: productId,
                MenuTitle: menuTitle
            },
            success: function (response) {
                if (response.exists) {
                    status
                        .text("Menu title already exists")
                        .addClass("text-danger");
                    menuTitleValid = false;
                } else {
                    status
                        .text("Menu title available")
                        .addClass("text-success");
                    menuTitleValid = true;
                }
            },
            error: function () {
                status
                    .text("Unable to validate menu title")
                    .addClass("text-danger");
                menuTitleValid = false;
            }
        });
    });



    document.addEventListener('DOMContentLoaded', function () {

        const isMainMenu = document.getElementById('IsMainMenu');
        const noSubMenus = document.getElementById('NoSubMenus');

        const parentMenuContainer = document.getElementById('parentMenuContainer');
        const pageContainer = document.getElementById('pageContainer');
        const productContainer = document.getElementById('productContainer');
        const iconContainer = document.getElementById('iconClassContainer');
        const displayOrderContainer = document.getElementById('displayOrderContainer');

        const parentSelect = document.getElementById('ParentMenuItemId');
        const pageSelect = document.getElementById('PageIds');
        const iconInput = document.getElementById('IconClass');
        const displayOrderInput = document.getElementById('DisplayOrder');
        const categoryContainer = document.getElementById('categoryContainer');
        const categorySelect = document.getElementById('CategoryId');


        function resetSelect(el, selector) {
            el.value = null;
            $(selector).val(null).trigger('change');
            el.removeAttribute('required');
        }

        function resetInputs() {
            iconInput.value = '';
            displayOrderInput.value = '';

            iconInput.removeAttribute('required');
            displayOrderInput.removeAttribute('required');
            pageSelect.removeAttribute('required');
            parentSelect.removeAttribute('required');
        }

    function toggleMenus() {

        resetInputs();

        // ALWAYS hide category by default
        categoryContainer.classList.add('d-none');
        categorySelect.removeAttribute('required');

        /* ===============================
           NO SUB MENUS
        =============================== */
        if (noSubMenus.checked) {

            isMainMenu.checked = true;

            parentMenuContainer.classList.add('d-none');

            productContainer.classList.remove('d-none');
            pageContainer.classList.remove('d-none');
            iconContainer.classList.remove('d-none');
            displayOrderContainer.classList.remove('d-none');

            // SHOW CATEGORY
            categoryContainer.classList.remove('d-none');
            categorySelect.setAttribute('required', 'required');

            resetSelect(parentSelect, "#ParentMenuItemId");

            pageSelect.setAttribute('required', 'required');
            iconInput.setAttribute('required', 'required');
            displayOrderInput.setAttribute('required', 'required');

            return;
        }

        /* ===============================
           MAIN MENU
        =============================== */
        if (isMainMenu.checked) {

            noSubMenus.checked = false;

            parentMenuContainer.classList.add('d-none');
            pageContainer.classList.add('d-none');

            productContainer.classList.remove('d-none');
            iconContainer.classList.remove('d-none');
            displayOrderContainer.classList.remove('d-none');

            // SHOW CATEGORY
            categoryContainer.classList.remove('d-none');
            categorySelect.setAttribute('required', 'required');

            resetSelect(parentSelect, "#ParentMenuItemId");
            resetSelect(pageSelect, "#PageIds");

            iconInput.setAttribute('required', 'required');
            displayOrderInput.setAttribute('required', 'required');

            return;
        }

        /* ===============================
           SUB MENU
        =============================== */
        noSubMenus.checked = false;

        parentMenuContainer.classList.remove('d-none');
        pageContainer.classList.remove('d-none');

        productContainer.classList.remove('d-none');
        iconContainer.classList.add('d-none');
        displayOrderContainer.classList.add('d-none');

        parentSelect.setAttribute('required', 'required');
        pageSelect.setAttribute('required', 'required');
    }


        toggleMenus();

        isMainMenu.addEventListener('change', toggleMenus);
        noSubMenus.addEventListener('change', toggleMenus);

        /* ===============================
           FORM SUBMIT
        =============================== */
        $("#RegisterForm").on("submit", function (e) {
            e.preventDefault();
            let form = this;

            if (!displayOrderValid) {
                Swal.fire({
                    icon: 'error',
                    title: 'Display Order Already Exists',
                    text: 'Please choose a different display order for this product.'
                });
                return; // stop form submission
            }

            if (!menuTitleValid) {
                Swal.fire({
                    icon: 'error',
                    title: 'Menu Title Already Exists',
                    text: 'Please choose a different menu title for this product.'
                });
                return; // stop form submission
            }

            if (!form.checkValidity()) {
                $(form).addClass('was-validated');
                return;
            }

            Swal.fire({
                title: 'Create Menu?',
                text: "Are you sure you want to create?",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Yes, Save',
                cancelButtonText: 'Cancel'
            }).then((result) => {

                if (!result.isConfirmed) return;

                Swal.fire({
                    title: 'Saving...',
                    allowOutsideClick: false,
                    didOpen: () => Swal.showLoading()
                });

                $.ajax({
                    url: '/Menu/Create',
                    type: 'POST',
                    data: $("#RegisterForm").serialize(),
                    headers: { "X-Requested-With": "XMLHttpRequest" },

                    success: function (response) {
                        Swal.close();

                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: response.message,
                                showConfirmButton: false,
                                timer: 1500
                            }).then(() => {
                                window.location.href = response.redirectUrl;
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Error',
                                text: response.message || 'Failed to create menu.'
                            });
                        }
                    },

                    error: function () {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: 'Something went wrong! Please try again.'
                        });
                    }
                });
            });
        });
    });


</script>


